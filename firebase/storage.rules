rules_version = '2';
// StudyOS no longer uses Firebase Storage (files → Cloudflare R2 via Worker).
// Rules left deny-all so a misconfigured bucket cannot accept writes.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
