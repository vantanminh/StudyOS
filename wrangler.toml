name = "studyos"
compatibility_date = "2026-08-01"
pages_build_output_dir = "dist"

# Cloudflare Pages deployment for StudyOS frontend.
# Future: Workers for signed R2 uploads and AI proxy if migrating off Firebase Functions.
# [vars]
# ENVIRONMENT = "production"
