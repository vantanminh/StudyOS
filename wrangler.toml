name = "studyos"
compatibility_date = "2026-08-01"

# Serve the Vite SPA as a Cloudflare Worker with Workers Static Assets.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
